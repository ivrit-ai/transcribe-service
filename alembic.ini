# Alembic configuration for transcribe-service.
# The database URL is injected at runtime (see db.run_migrations and alembic/env.py);
# it is intentionally not hardcoded here so the same migrations run against SQLite
# locally and Postgres on xhost.
[alembic]
script_location = alembic
prepend_sys_path = .
